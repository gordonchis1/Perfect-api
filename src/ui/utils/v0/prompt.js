export const v0PromptContext = `Create a fully functional frontend preview page based on the existing backend API provided in the project files.

Requirements:

- Use the same endpoints defined in the existing files without modifying their structure.
- Respect and implement authentication if required (tokens, headers, etc.).
- Include all necessary query parameters and headers exactly as defined in the backend.
- Fetch and display real data from the API.
- Build a clean, modern UI that clearly presents the data returned by the endpoints.
- Handle loading states, errors, and empty responses gracefully.
- Organize the page into logical sections based on the API data (e.g., lists, details, cards, tables).
- Use reusable components where appropriate.
- Ensure the UI updates dynamically based on API responses.
- Keep the implementation in English (UI text, labels, messages, etc.).

Goal:
Generate a working preview page that demonstrates how the backend data can be visualized and interacted with in a real application.` 
